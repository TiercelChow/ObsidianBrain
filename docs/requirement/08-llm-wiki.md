# 阅境轩·书籍知识库（Book Wiki）— 需求设计文档 v3

> **文档编号**: REQ-08
> **版本**: v3.0
> **状态**: 方案确认
> **最后更新**: 2026-09-12
> **上游模块**: 阅境轩书架（REQ-10）
> **替代范围**: 原知识库、Wiki 看板、Wiki 工作台、知识探索、外部摄入五个页面及旧 Wiki 工作流

---

## 1. 产品定位

### 1.1 一句话定义

以阅境轩书架为入口，把每一本 PDF 或 Markdown 文集维护为一个相互隔离、可持续演化的 LLM Wiki 知识库。

### 1.2 核心关系

```text
阅境轩负责阅读原书
    ↓
书籍知识库负责理解、组织、检索和维护知识
    ↓
Agent Harness 负责分析、问答和研究任务的执行
```

每一本书都是独立知识库。默认情况下，一本书的实体、检索结果、对话、任务和 Agent 上下文不得进入另一本书。跨书查询必须由用户显式选择参与的知识库。

### 1.3 LLM Wiki 原则在本产品中的映射

| LLM Wiki 原则 | 本产品实现 |
|---|---|
| 原始资料保持可追溯 | 阅境轩书籍路径只读；记录来源版本、哈希、页码、标题与行号 |
| 知识持续积累 | 结构化实体、论断、关系、引用和综合内容写入 SQLite |
| Purpose 决定方向 | 每个知识库有独立 purpose 配置文档 |
| Schema 约束结构 | 每个知识库有独立 schema 配置文档和实体类型约束 |
| Ingest / Query / Lint 闭环 | 摄入、问答、维护均通过任务与审核机制进入同一知识模型 |
| 人负责判断，Agent 负责维护 | 高风险变更必须审核；Agent 不能直接执行 SQL 或绕过权限 |

### 1.4 核心价值

1. 阅读不再止于打开文件，而会逐步形成可检索、可验证的书籍知识体系。
2. 每个回答都能回到原书的具体页码、文件、标题或文本片段。
3. 知识以数据库实体管理，支持筛选、合并、版本、图谱和结构化检索。
4. 长时间摄入、研究和维护由成熟 Agent Harness 执行，不在业务层重复实现 Agent Loop。
5. 知识内容、业务状态和安全边界由 ObsidianBrain 掌控，Agent Runtime 可以替换。

---

## 2. 范围与非目标

### 2.1 首版范围

- 将现有书架条目升级为稳定的书籍身份。
- 为 PDF 或 Markdown 文件夹建立一对一知识库。
- 管理知识库的初始化、同步、暂停、健康、审核、归档和删除。
- 从原书提取来源文档和可定位片段。
- 生成并维护实体、论断、关系、引用、综合内容和版本历史。
- 提供单书问答、显式跨书问答和回答回写。
- 提供可由 Agent 执行的研究任务。
- 提供 LLM、Harness、配置文档、Skill、模板和安全策略管理。
- 首个 Agent Runtime 接入 DeepSeek Harness，同时保留 Claude Code Adapter 边界。
- 支持桌面与手机端完整操作。

### 2.2 明确非目标

- 不继续维护旧的全 Vault 知识洞察、随机概念碰撞和雷达专用摄入页面。
- 不把原书复制或改写为 Wiki Markdown 目录作为主存储。
- 不让 Agent 直接修改 SQLite、原始书籍或正式知识实体。
- 不在第一阶段实现任意网站爬取、通用 RAG 平台或多人协作权限系统。
- 不把研究任务和现有个人待办强行合并；只提供可选关联。
- 不承诺首版支持 EPUB、Word、PPT 等阅境轩当前不支持的格式。

---

## 3. 信息架构

“知识”分组保留五个入口，但全部替换为新页面：

| 页面 | 路由建议 | 职责 |
|---|---|---|
| 知识库 | `/knowledge` | 管理书架中所有书籍知识库的状态与生命周期 |
| Wiki 工作台 | `/knowledge/:id/wiki` | 查看实体、来源、关系、审核和运行记录 |
| 书籍问答 | `/knowledge/chat` | 基于一个或多个显式选中的书籍进行带引用问答 |
| 研究任务 | `/knowledge/tasks` | 管理摄入、验证、综合、维护和研究任务 |
| Wiki 配置 | `/knowledge/settings` | 管理 Harness、模型、配置文档、Skill、模板和安全策略 |

从知识库列表、书架卡片或问答结果进入 Wiki 工作台时，当前知识库必须明确显示，不允许以隐含的“最近一本书”作为写操作目标。

---

## 4. 核心领域概念

### 4.1 书籍（Reader Book）

阅境轩中的阅读对象：一个 Markdown 文件夹或一个 PDF 文件。书籍可以尚未启用知识库。

### 4.2 知识库（Knowledge Base）

与书籍一对一绑定的逻辑空间。知识库拥有独立配置、来源版本、实体、关系、对话、任务、审核和运行记录。

### 4.3 来源文档与来源片段

- PDF 书：整本 PDF 是来源文档，页面或页面内段落是来源片段。
- Markdown 文集：每个 Markdown 文件是来源文档，标题区块或分段是来源片段。
- 每个片段必须能够跳回阅境轩中的原位置。

### 4.4 知识条目（Knowledge Entry）

可独立查看和管理的知识对象，类型至少包括：

- 章节摘要 `chapter`
- 人物、组织、地点、作品等实体 `entity`
- 概念 `concept`
- 方法 `method`
- 事件 `event`
- 对比 `comparison`
- 综合论述 `synthesis`
- 问题 `question`
- 已保存回答 `answer`
- 全书概览 `overview`

条目正文以 Markdown 字符串存入数据库，用现有安全 Markdown 渲染方案展示；条目不是磁盘上的 Markdown 文件。

### 4.5 论断（Claim）

可独立验证的最小知识陈述。论断必须可以关联来源、支持证据、反对证据、可信度和验证状态。

### 4.6 关系与引用

- 关系表达条目之间的语义连接，如“属于”“支持”“反驳”“解释”“比较”“前置知识”。
- 引用连接条目或论断与原书来源片段。
- 图谱来自数据库关系，不依赖解析 `[[wikilink]]`。

### 4.7 变更集与审核

Agent 的新增、修改、合并、拆分、归档建议必须形成结构化变更集。变更集通过校验后自动应用或进入人工审核。

### 4.8 研究任务

知识工作流中的可执行事项，区别于个人任务中枢的日程型任务。研究任务可以选择关联到个人任务，但知识结果仍归属书籍知识库。

---

## 5. 知识库状态模型

知识库状态拆分为三个正交维度，避免单一枚举表达不清。

### 5.1 生命周期

```text
uninitialized → active ⇄ paused → archived
```

- `uninitialized`：书已在书架，但尚未建库。
- `active`：允许同步、问答和执行任务。
- `paused`：保留全部数据，停止自动同步和新任务。
- `archived`：只读保留，不进入默认列表。

### 5.2 同步状态

```text
clean | outdated | scanning | extracting | ingesting | failed
```

### 5.3 健康状态

```text
healthy | warning | needs_review
```

健康状态由确定性检查得出，不使用不可解释的单一 LLM 分数。主要指标包括：

- 无来源论断数量
- 待验证论断数量
- 矛盾论断数量
- 孤立实体数量
- 重复实体候选数量
- 失效来源引用数量
- 待审核变更数量
- 失败任务数量

---

## 6. 页面需求

### 6.1 知识库页面

#### 页面目标

快速看清所有书籍知识库当前是否可用、是否需要同步、是否需要人工处理。

#### 顶部摘要

- 已启用知识库
- 正在处理
- 原书有变化
- 待审核
- 运行失败

#### 主列表

支持书架卡片和高密度列表两种视图；桌面记住用户选择，手机默认卡片。

每项显示：

- 书名、类别、封面色和阅读进度
- 生命周期、同步和健康状态
- 最近同步时间
- 来源文档、知识条目和论断数量
- 有来源论断比例
- 待审核和失败数量
- 当前模板、Harness 和模型概要
- 运行中任务及进度

#### 操作

- 初始化知识库
- 打开 Wiki 工作台
- 问这本书
- 扫描原书变化
- 增量同步
- 暂停或恢复
- 执行健康检查
- 重新构建
- 归档
- 删除知识库

批量操作首版支持扫描、同步、暂停和恢复。重新构建与删除不允许批量静默执行。

### 6.2 Wiki 工作台

#### 桌面布局

- 左侧：类型、标签、状态、来源和搜索导航。
- 中间：知识条目或来源片段正文。
- 右侧：引用、关系、论断、版本和快捷操作。

左栏允许压缩为窄轨；右栏允许关闭。中间内容始终获得主要宽度。

#### 手机布局

- 默认只显示主体内容。
- 导航、关系、版本和操作通过底部抽屉打开。
- 不把桌面三栏直接压缩到手机宽度。

#### 主要视图

- 概览：全书目的、概览、关键概念、健康和最近变化。
- 实体：按类型和状态浏览知识条目。
- 来源：查看文件、PDF 页和抽取状态。
- 图谱：查看关系；手机默认先显示洞察列表，再进入全屏图谱。
- 审核：查看 Agent 变更差异并批准、驳回或编辑。
- 运行：查看任务步骤、工具调用、Token、耗时和错误。

#### 实体详情

实体详情至少展示：

- 标题、类型、别名、状态、可信度
- Markdown 正文
- 原子论断及验证状态
- 来源与原文位置
- 关联实体和关系理由
- 支持与反对证据
- 相关问答和研究任务
- 版本历史

支持编辑、合并、拆分、添加别名、标记过时、提问和创建验证任务。

### 6.3 书籍问答

#### 查询范围

- 默认单书，必须有明确的知识库选择。
- 跨书问答需要用户勾选多个知识库，并在会话头部持续展示范围。
- 阅读上下文入口自动带入当前书籍、文件、标题、页码和选中文字。

#### 回答要求

- 流式显示正文。
- 引用知识条目和原始来源片段。
- 点击引用跳回阅境轩的 PDF 页码或 Markdown 标题。
- 区分“直接来源支持”“知识库综合”“Agent 推断”。
- 当证据不足或冲突时明确提示。

#### 后续操作

- 保存为知识条目
- 更新已有知识条目
- 创建研究或验证任务
- 标记回答有用、存疑或错误
- 导出 Markdown

任何回写都必须形成变更集，不允许聊天结果直接覆盖正式知识。

### 6.4 研究任务

#### 任务类型

- 建库初始化
- 增量摄入
- 全书或章节总结
- 实体提取与去重
- 论断验证
- 概念比较
- 知识缺口研究
- 健康检查与修复
- 自定义研究

#### 状态

```text
draft → queued → running → waiting_review → completed
                       ├──→ failed
                       └──→ cancelled
```

#### 任务内容

- 所属知识库和来源范围
- 目标、约束和验收说明
- Harness、模型、配置文档和 Skill 版本快照
- 执行步骤、实时进度和当前等待事项
- Token、耗时、重试次数和错误
- 生成的变更集和最终结论

用户可以取消运行中任务、重试失败任务、编辑草稿任务、批准变更后继续任务。

### 6.5 Wiki 配置

配置按以下层级合并：

```text
系统默认 → 场景模板 → 单书覆盖 → 单次运行覆盖
```

页面必须显示每个最终值来自哪个层级，并支持恢复继承。

#### Agent Runtime

- DeepSeek Harness
- Claude Code
- 后续自定义 Harness
- 可执行程序、版本和健康检查
- Profile、最大轮次、并发、超时和取消策略
- 沙箱、网络、Shell、文件系统和审批策略

#### 模型配置

- Provider、Base URL、Model
- API Key 的环境变量或系统密钥引用
- 推理模式、温度、上下文和最大输出
- 超时、重试和 Token 预算
- 可分别配置摄入、问答、维护和研究模型

#### 配置文档

以文件式标签编辑，但永久存储在数据库：

- `purpose.md`
- `schema.md`
- `instructions.md`
- `AGENTS.md`
- `CLAUDE.md`

每次保存形成版本；支持差异、恢复和模板重置。

#### Skill 管理

- 内置、导入和自定义 Skill
- Skill 文件树与 `SKILL.md` 编辑
- 版本、来源、内容哈希和权限声明
- 全局启用和单书启用
- 依赖工具检查
- 运行历史和最近使用时间

首版内置：`book-ingest`、`book-query`、`book-lint`、`book-research`、`book-synthesis`、`markdown-collection`、`pdf-extract`。

#### 处理策略

- 来源变化检测与自动同步
- PDF 文本提取和可选 OCR
- 分块策略
- 低风险自动应用范围
- 高风险强制审核范围
- 备份、保留和导出策略

---

## 7. 阅境轩集成

### 7.1 书架

书架卡片增加轻量状态标记：未建库、构建中、已就绪、有更新、待审核、失败。

卡片菜单增加：

- 建立知识库
- 打开 Wiki
- 问这本书
- 增量同步
- 暂停知识库

添加书籍不默认启动耗时建库。用户可在添加流程中勾选“立即建立知识库”，或稍后从书架和知识库页面初始化。

### 7.2 阅读页

选中文字后的上下文操作：

- 询问
- 解释
- 添加为知识候选
- 创建研究任务

PDF 带入页码，Markdown 带入文件和标题。阅读页不常驻复杂 Wiki 面板，避免压缩正文。

### 7.3 删除关系

- 删除书籍时询问保留、归档或删除知识库。
- 删除知识库不删除书籍及原始文件。
- 选择保留或归档知识库时，书籍记录采用软删除，仅从书架隐藏，以维持来源、阅读跳转和知识库外键完整性。
- 原书路径失效时知识库进入警告状态，已有知识仍可只读查看。

---

## 8. 数据权威与导入导出

### 8.1 唯一事实来源

| 数据 | 权威存储 |
|---|---|
| 原始 PDF / Markdown | 用户本地文件 |
| 来源版本和抽取片段 | SQLite |
| 知识条目正文 | SQLite Markdown 字段 |
| 论断、关系和引用 | SQLite 结构化表 |
| 对话、任务、审核和运行 | SQLite |
| Purpose、Schema 等配置文档 | SQLite Markdown 字段 |
| Skill 内容和版本 | SQLite |
| Agent 临时配置文件 | 临时工作区，不是权威数据 |
| Obsidian / 标准 LLM Wiki 目录 | 按需导出 |

禁止同时把数据库与 `wiki/*.md` 作为可编辑主存储。

### 8.2 导出

每个知识库支持导出：

- 可浏览的 Markdown Wiki
- 结构化 JSON 包
- 数据库备份包
- 引用清单
- 问答与任务报告

导出是单向快照，不自动反向同步。

### 8.3 备份

因为知识数据库不能从原书完整重建，必须提供：

- SQLite 在线一致性备份
- 迁移前自动备份
- 可配置保留数量
- 恢复前完整性检查
- 单书 JSON/Markdown 可移植导出

---

## 9. Agent Runtime 产品要求

### 9.1 Runtime 与模型解耦

Harness 是 Agent 执行环境，模型是推理提供方。配置和运行记录必须分别保存。

首版正式支持：

- DeepSeek Harness Adapter
- Claude Code Adapter

### 9.2 DeepSeek Harness

- 作为固定版本的 Node.js Sidecar 运行。
- 使用专用 `obsidianbrain-wiki` Profile，只加载必要插件。
- 通过 ACP/stdio 管理会话、提示、审批和取消。
- 通过 ObsidianBrain 专用工具插件访问知识能力。
- 不使用 Harness 自身存储作为产品业务事实来源。

### 9.3 Claude Code

- 通过 Agent SDK 或非交互 CLI 运行。
- 使用显式工作目录、Skill、MCP、权限和输出格式。
- 不自动继承用户目录中与本次知识任务无关的配置。

### 9.4 统一行为

两种 Runtime 必须遵循同一业务工具契约、变更集契约和审核策略。切换 Runtime 不迁移知识数据。

---

## 10. 安全与隐私

- 服务继续仅监听 `127.0.0.1`。
- 每次 Agent 运行使用短期、仅限单书和单次运行的能力令牌。
- Agent 不能看到其他知识库，除非跨书任务明确授权。
- API Key 不进入 Markdown、日志、聊天正文或普通数据库字段。
- 原书目录默认为只读。
- Agent 不获得数据库文件路径和 SQL 工具。
- 所有外部写操作、权限提升、删除与高风险变更均可审计。
- 日志输出必须对密钥、请求头和敏感路径做脱敏。

---

## 11. 非功能要求

### 11.1 性能

- 知识库列表首屏 P95 < 300ms，不等待 Agent 或全量统计。
- 单书实体列表 P95 < 200ms。
- FTS 首批搜索结果 P95 < 300ms（十万来源片段规模目标）。
- 打开实体正文 P95 < 150ms，不含外部资源。
- Agent 任务全部异步，不阻塞页面请求。
- 前端长列表必须分页或虚拟化。
- 没有运行任务时不常驻 Harness Sidecar；允许短暂保温，但到期后必须自动退出，使基础应用空闲资源目标不因 Book Wiki 被永久抬高。

### 11.2 可靠性

- Agent 重复提交同一变更不能产生重复实体。
- 所有知识变更在单个 SQLite 事务中提交。
- 崩溃恢复后运行任务不得被误标为成功。
- 来源扫描和摄入支持取消、重试和幂等恢复。
- 版本冲突不得静默覆盖用户修改。

### 11.3 可观测性

每次运行记录：

- 知识库、任务、Harness、模型和版本
- 配置文档与 Skill 快照哈希
- 开始、结束、耗时和状态
- 工具调用摘要
- Token 与重试
- 产生的变更集
- 错误类别和可恢复建议

---

## 12. 旧功能处置与迁移

### 12.1 前端

迁移完成后删除：

- `Memory.vue`
- `WikiDashboard.vue`
- `WikiWorkbench.vue`
- `Explore.vue`
- `Ingest.vue`

旧路由统一跳转到 `/knowledge`，保留一个发布周期后移除跳转。

### 12.2 后端

迁移完成后删除或退役旧 Wiki、Explore 和 Knowledge Insights 工具。旧 `Wiki/*.md` 不自动导入；如用户需要，提供独立的一次性导入器，不让兼容逻辑污染新领域模型。

### 12.3 书架迁移

- 将 `app_state.reader_books` 迁移到正规 `reader_books` 表。
- 保留原 ID、路径、展示信息和阅读进度。
- 所有现有书籍初始 `wiki_enabled = false`。
- 迁移成功后保留旧 JSON 备份键一个发布周期。

---

## 13. 验收标准

1. 书架中每本书最多绑定一个知识库，数据库层强制唯一。
2. 两本书同时建库时，实体、会话、检索和 Agent 工具访问完全隔离。
3. PDF 来源引用可跳到正确页码；Markdown 引用可跳到正确文件和标题。
4. 修改 Markdown 文件或替换 PDF 后，知识库进入 `outdated`，增量同步只处理变化来源。
5. Agent 生成实体、论断、关系和引用后，必须通过结构校验与事务写入。
6. 高风险变更未经审核不能改变正式知识。
7. 实体支持全文检索、类型筛选、来源筛选、状态筛选和关系导航。
8. 问答回答包含可点击引用；证据不足时不能伪造来源。
9. 研究任务支持排队、进度、取消、失败重试和审核后继续。
10. 配置页可以管理继承关系、配置文档、Skill 和不同任务模型。
11. 切换 DeepSeek Harness 与 Claude Code 不需要迁移知识数据。
12. 删除知识库不影响书籍和原始文件；删除书籍时必须明确选择知识库处置方式。
13. 数据库备份可恢复出实体、引用、配置、对话、任务和版本。
14. 桌面和手机端均能完成建库、搜索、问答、审核和任务管理主流程。

---

## 14. 实施优先级

### Phase A：领域底座

书架正规化、知识库生命周期、来源模型、数据库迁移、备份和基础 API。

### Phase B：摄入闭环

Markdown/PDF 抽取、增量检测、变更集、审核、实体/论断/关系/引用和 FTS。

### Phase C：DeepSeek Harness

Sidecar、专用 Profile、工具桥、Skill 物化、运行队列、进度、取消和权限。

### Phase D：五个新页面

知识库、Wiki 工作台、书籍问答、研究任务、Wiki 配置及阅境轩联动。

### Phase E：Claude Code 与导出

Claude Code Adapter、跨书查询、Markdown/JSON 导出和完整恢复验证。

### Phase F：旧模块移除

路由迁移、旧前后端删除、文档与产品介绍更新、性能与移动端回归。

---

## 15. 修订历史

| 版本 | 日期 | 说明 |
|---|---|---|
| v2.0 | 2026-06-24 | 全 Vault Wiki 看板、工作台、探索和外部摄入方案 |
| v3.0 | 2026-09-12 | 重构为阅境轩每书一库；SQLite 为生成知识唯一事实来源；引入实体、论断、关系、审核、研究任务及 DeepSeek Harness/Claude Code 双 Runtime |

## 16. 参考资料

- [Karpathy LLM Wiki 方法文档](https://github.com/nashsu/llm_wiki/blob/main/llm-wiki.md)
- [nashsu/llm_wiki 项目说明](https://github.com/nashsu/llm_wiki/blob/main/README_CN.md)
- [DeepSeek Harness 官方介绍](https://deepseek.com/harness/en/)
- [DeepSeek Harness ACP](https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/acp/acp/README.md)
- [DeepSeek Harness Skill 子系统](https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/subsystems/skills.md)
- [Claude Code 程序化运行](https://code.claude.com/docs/en/headless)
