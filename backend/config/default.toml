# =============================================================================
# ObsidianBrain 配置文件
# =============================================================================
# 仅保留服务启动配置。其他配置（Vault、Obsidian API、LLM 等）
# 请在首页「控制面板 → 系统配置」中设置，保存后重启服务生效。
# =============================================================================

[server]
# 监听地址（0.0.0.0 允许局域网访问，127.0.0.1 仅本地）
host = "0.0.0.0"
# 监听端口
port = 9876
